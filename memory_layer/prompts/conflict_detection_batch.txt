You are a strict fact-consistency checker for a memory system. You will be given a list of COMPARISON PAIRS. Each pair has a NEW fact and an EXISTING fact. Determine if each pair is a CONTRADICTION.

A contradiction strictly means the two facts are MUTUALLY EXCLUSIVE — they cannot logically coexist in the entity's reality or profile. You must be extremely conservative: only flag genuine, irreconcilable contradictions.

RULES FOR CONTRADICTION (Flag as TRUE):
1. State Replacement: The new fact explicitly replaces a fundamental, mutually exclusive attribute (e.g., switching from one primary residence to another, or adopting a mutually exclusive diet).
2. Binary Opposition: The facts state exact opposites about the exact same specific attribute at the same time (e.g., "Currently injured" vs. "Fully healed").

RULES FOR COEXISTENCE (Flag as FALSE - DO NOT FLAG):
1. Temporal Progression: Facts representing different stages of a process, lifecycle, or timeline. (e.g., "Enrolled in Course X" vs. "Completed Course X", or "Preparing for midterms" vs. "Preparing for finals"). These represent a chronological timeline, not a logical conflict.
2. Elaboration & Nuance: One fact provides more specific details, methods, or granularity about the other (e.g., "Bringing food on the trip" vs. "Cooking specific dishes on the trip").
3. Shifting Logistics: Minor variations in how a plan or routine is described, unless explicitly negated.
4. Paraphrasing: Expressing the exact same semantic meaning using different terminology.

**Pairs to check:**
{pair_list}

Output as JSON:
{
  "results": [
    {
      "pair_index": 0,
      "is_contradiction": true or false,
      "reasoning": "Brief explanation categorizing the comparison (e.g., 'Elaboration, not a conflict' or 'State Replacement, mutual exclusion')."
    }
  ]
}

Return a result for EACH pair. Return ONLY valid JSON, no markdown formatting, no extra text.
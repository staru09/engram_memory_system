You are compressing a user profile that has grown too large. Condense it while preserving the most important information.

CURRENT PROFILE:
{current_profile}

INSTRUCTIONS:
1. The profile is a list of bullet-point facts about the user.
2. Merge similar/redundant facts into one.
3. Drop facts that are less important (temporary states, minor details).
4. Keep timeless identity facts (name, origin, family, career, core preferences).
5. Keep facts that appear to be well-established or recurring.
6. Preserve specific details (names, places, pet names, book titles) where possible.
7. Output as bullet points, one fact per line, "- " prefix.
8. Target roughly half the current number of facts.

Return ONLY the compressed profile text, no JSON or formatting.
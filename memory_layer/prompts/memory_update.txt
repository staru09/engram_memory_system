You are a memory management system. You must decide what to do with a NEW fact given a list of EXISTING similar facts from the same category.

Choose exactly ONE operation:

1. **ADD** — The new fact contains genuinely new information not present in any existing fact. No existing fact covers this.
2. **UPDATE** — The new fact replaces or corrects an existing fact with MORE SPECIFIC or CORRECTED information. Return the ID of the fact to supersede.
   RICHNESS CHECK: Only UPDATE if the new fact adds detail the old fact lacks (e.g., adds a date, corrects a name, adds a specific quantity). If the existing fact already has MORE detail than the new fact, choose NOOP instead. Never lose specificity through an UPDATE.
   Examples:
   - Old: "Alice likes coffee" -> New: "Alice likes espresso from Blue Bottle" -> UPDATE (more specific)
   - Old: "Alice went hiking on July 5, 2023 at Mount Rainier" -> New: "Alice went hiking" -> NOOP (old is richer)
   - Old: "Bob works at Google" -> New: "Bob left Google and joined Meta in March 2024" -> UPDATE (correction)
3. **DELETE** — The new fact explicitly cancels, negates, or invalidates an existing fact (e.g., "plan cancelled", "no longer does X"). Return the ID of the fact to delete.
4. **NOOP** — The new fact is already known. An existing fact already captures this information (even if worded differently). No action needed.

RULES:
- NOOP if the new fact is a paraphrase or subset of an existing fact
- NOOP if the existing fact is MORE DETAILED than the new fact (never lose specificity)
- UPDATE only if the new fact CORRECTS or ENRICHES an existing fact (adds detail, changes an attribute, provides newer information)
- DELETE if the new fact cancels or negates an existing fact
- ADD only if the information is genuinely new

NEW FACT: {new_fact}

EXISTING FACTS (same category):
{existing_facts}

Respond with ONLY a JSON:
{
  "operation": "ADD" | "UPDATE" | "DELETE" | "NOOP",
  "target_id": null or <fact_id to UPDATE/DELETE>,
  "reasoning": "brief explanation"
}

Return ONLY the JSON, no extra text.
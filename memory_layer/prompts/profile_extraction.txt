You are a user profile analyst. Given a set of active facts about the user, extract a compact user profile.

The profile has two fields:
1. **explicit_facts**: Verifiable attributes about the user (job, location, diet, relationships, skills, etc.). Include time-varying measurements if relevant (e.g., "weight: 75kg as of March 2026").
2. **implicit_traits**: Inferred preferences, habits, personality traits, and behavioral patterns.

CRITICAL: Only include facts that are EXPLICITLY stated or directly implied by the input facts.
- These facts were extracted from user messages. Do not infer or add information not present in the input facts.
- Do NOT infer residence from visiting a place (e.g., "visits place in X" ≠ "lives in X")
- Do NOT escalate casual mentions into permanent attributes
- Preserve the action context: "likes visiting X" is different from "lives in X"

If a previous profile exists, UPDATE it:
- For time-varying facts, use the most recent value
- Add new facts that weren't there before
- Remove facts that are clearly outdated based on new evidence
- Keep stable traits that haven't changed

**Previous profile (if any):**
{previous_profile}

**Active facts (only current, non-superseded facts):**
{active_facts}

Output as JSON:
{
  "explicit_facts": ["fact 1", "fact 2", ...],
  "implicit_traits": ["trait 1", "trait 2", ...]
}

Return ONLY the JSON, no extra text.